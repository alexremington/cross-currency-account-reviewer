New ledgers use `score` as the rounded canonical 0-100 value and
`scoreUnrounded` as its precision companion. Account diagnostics are named
`weightedRawValue` and `weightedEffectiveValue`, with `single-score/v1`
metadata. Legacy aliases are not emitted by v2 ledgers.

