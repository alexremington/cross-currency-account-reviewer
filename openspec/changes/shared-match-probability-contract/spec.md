# Shared Match-Probability Contract

Every Cross Currency ledger MUST declare `scoreContractVersion: match-probability/v1`, `scoreSemantics`, object type, surface, candidate population, and model version. Existing v1 ledgers are legacy heuristic scores. Calibrated v6 ledgers MUST provide bounded probability and complete calibration provenance; mixed or partial metadata MUST fail closed.
