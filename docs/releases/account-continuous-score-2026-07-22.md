# Account continuous score

Release note: Cross Currency Account scores now use the continuous weighted evidence estimate; exact-confidence and intermediate-confidence remain advisory metadata, while invalid typed Website/Phone values remain auditable and unavailable.

Validated by unit tests, structural and Windows checks, result sanity checks, and Playwright UI smoke.
